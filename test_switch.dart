import 'package:flutter/material.dart';

void main() {
  const Switch(
    value: true,
    onChanged: null,
    activeThumbColor: Colors.blue,
  );
  print('Switch with activeThumbColor compiled successfully');
}
